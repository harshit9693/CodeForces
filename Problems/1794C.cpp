#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void solve()
{

  ll n;
  cin >> n;
  vector<ll> arr(n);
  for (ll i = 0; i < n; i++)
  {
    cin >> arr[i];
  }

  ll prod = 1, len = 1;
  vector<ll> ans(n);
  for (ll i = 0; i < n; i++)
  {
    if ((prod * arr[i]))
    {
    }
  }
}

int main()
{

  ll t;
  cin >> t;

  while (t--)
  {
    solve();
  }
}